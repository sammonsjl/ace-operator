---
collections:
  - name: kubernetes.core
    version: ">=3.0.0"
  - name: operator_sdk.util
    version: ">=0.5.0"
  # The gateway programs an HTTPS listener into envoy that references a
  # certificate; without one envoy rejects every LDS update and opens no
  # listener at all.
  - name: community.crypto
    version: ">=2.20.0"
