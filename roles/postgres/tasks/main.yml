---
# Managed postgres is a convenience for a lab, not a production database. A real
# deployment points spec.postgres.host at something with backups, in which case
# this role does nothing at all.

- name: Deploy the managed database
  ansible.builtin.include_tasks: managed.yml
  when: postgres.managed | default(true)
